{ "step": "send prompt",    "do":     { "kind": "sendPrompt",          "params": { "text": "hi" } } }
{ "step": "turn ends",      "assert": { "kind": "waitForTurn",         "params": { "status": "done" } } }
{ "step": "got text",       "assert": { "kind": "expectAssistantText" }, "expect": { "regex": "hello" } }
