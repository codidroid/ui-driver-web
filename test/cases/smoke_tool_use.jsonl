{ "step": "send prompt",    "do":     { "kind": "sendPrompt",          "params": { "text": "read /tmp/x" } } }
{ "step": "turn ends",      "assert": { "kind": "waitForTurn",         "params": { "status": "done" } } }
