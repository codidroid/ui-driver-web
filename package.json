{
  "name": "ui-driver-web",
  "version": "0.1.0",
  "private": true,
  "description": "JSONL-case-driven Playwright runner for any web project. Standalone — pass --backend-bin, --cases-root, --commands, --fakecc-bin, etc. on the command line.",
  "scripts": {
    "build": "tsc -b",
    "server": "tsx src/bin.ts",
    "runner": "tsx test/runner.ts",
    "runner:all": "node test/run-all.js",
    "runner:chromium": "node test/run-all.js --browser chromium",
    "runner:firefox": "node test/run-all.js --browser firefox",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "express": "^4.19.0",
    "playwright": "^1.45.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "vitest": "^1.6.0"
  }
}
